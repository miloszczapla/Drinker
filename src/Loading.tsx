import icon from './assets/img/loadingicon.svg';

const Loading = () => {
  return (
    <section className='loading'>
      <img src={icon} alt='Loading...' />
    </section>
  );
};

export default Loading;
